package org.hw.greet

/** Composes a greeting message for the given [name]. */
fun greet(name: String): String = "Hello, $name! (from the :greet subproject)"

/** Adds two integers. */
fun add(a: Int, b: Int): Int = a + b

